const httpVerbs = ['POST', 'GET', 'PUT', 'PATCH', 'DELETE'];
const requestRegex = new RegExp(`(${httpVerbs.join('|')})\\s(.*)`, 'i');
const replacementRegex = /(\$[a-zA-Z\.]*)/g;

module.exports = {
	httpVerbs,
	requestRegex,
	replacementRegex
};
